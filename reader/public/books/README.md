# Book Manifests

Place processed `.manifest.json` files here.

Run from the `pipeline/` directory:

```bash
npm install
npm run ingest -- time-machine
npm run ingest -- alice
npm run ingest -- moby-dick
```
